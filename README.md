# Табло соревнования

Локальное приложение для управления результатами и трансляции в формате 1920×1080. Интернет и Excel не требуются: все данные вводятся в админке и сохраняются в `data/scoreboard.json`.

## Запуск

1. Дважды щёлкните `start.bat`.
2. Откройте `http://localhost:3000/admin`.
3. При необходимости отредактируйте организации и участников.
4. Во вкладке «Таблица результатов» заполните теорию и 13 критериев практики.
5. Отметьте участников финала и заполните для них 13 финальных критериев.
6. Нажмите «Сохранить изменения» — эфирные экраны обновятся автоматически.

Эфирные страницы:

- `/theory` — теоретический этап;
- `/practice` — практический этап;
- `/final` — финал и итог;
- `/admin` — организации, участники и результаты.

Перед каждым сохранением создаётся резервная копия JSON в `data/backups`.

Итог участника рассчитывается автоматически: теория + практика + финал. Итог организации — сумма результатов всех её участников. Теория ограничена 60 баллами, остальные поля принимают любые неотрицательные баллы.

## Разработка

Требуется Node.js 22 или новее.

```powershell
npm.cmd install
npm.cmd run dev
```

Vite работает на `http://localhost:5173`, API — на `http://localhost:3000`.

Проверка и production-сборка:

```powershell
npm.cmd run check
npm.cmd run build
```

Создание переносимого офлайн-комплекта:

```powershell
npm.cmd run release
```
